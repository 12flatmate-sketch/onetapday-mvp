# Технический отчёт по рефакторингу проекта OneTapDay MVP

## 📋 Структура проекта

### Текущая структура

```
onetapday-mvp/
├── server.js                    # Backend (Express, 728 строк)
├── package.json                 # Зависимости (express, stripe, cookie-parser, crypto)
├── config/                      # Пустая папка
├── data/uploads/                # Загруженные файлы
├── uploads/                     # Дубликат uploads
├── public/
│   ├── index.html              # Лендинг доступа
│   ├── app.html                # Основное приложение (940 строк HTML)
│   ├── main.js                 # Логика лендинга (496 строк)
│   ├── sync-cloud.js           # Firebase синхронизация (258 строк)
│   ├── manifest.json            # PWA манифест
│   ├── js/
│   │   └── app-main.js         # ⚠️ МОНОЛИТ: 2961 строка!
│   ├── i18n_pack/
│   │   ├── i18n.js             # Простой i18n (24 строки, не используется)
│   │   └── i18n/
│   │       ├── pl.json         # JSON переводы
│   │       ├── en.json
│   │       ├── ru.json
│   │       └── uk.json
│   └── [иконки, изображения]
└── node_modules/
```

### Ключевые модули

1. **server.js** (728 строк)
   - Express сервер
   - Аутентификация (JWT-like сессии)
   - Управление пользователями (users.json)
   - Stripe интеграция
   - API endpoints для app-state
   - Загрузка изображений

2. **public/js/app-main.js** (2961 строка) ⚠️
   - **God Component** - содержит ВСЁ:
     - UI рендеринг
     - Бизнес-логику
     - Работу с localStorage
     - API вызовы
     - Синхронизацию с Firebase
     - i18n
     - Обработку файлов (CSV, OCR)
     - Голосовой ввод
     - Графики и визуализации

3. **public/main.js** (496 строк)
   - Логика лендинга (регистрация/логин)
   - Дублирует часть функционала app-main.js
   - Собственная i18n система

4. **public/sync-cloud.js** (258 строк)
   - Firebase синхронизация
   - Адаптер для app.html

---

## 🔍 Проблемы и наблюдения

### 1. Архитектурные проблемы

#### 1.1 Монолитный файл (app-main.js)
- **2961 строка** в одном файле
- Нарушение **Single Responsibility Principle**
- Смешаны: UI, бизнес-логика, работа с данными, синхронизация
- Невозможно тестировать отдельные части
- Сложно поддерживать и расширять

#### 1.2 Дублирование кода

**i18n системы:**
- `app-main.js` - объект `M` с переводами (500+ строк)
- `main.js` - объект `T` с переводами (50 строк)
- `i18n_pack/i18n.js` - не используется
- `i18n_pack/i18n/*.json` - JSON файлы, частично используются

**Работа с localStorage:**
- `app-main.js`: `loadLocal()`, `saveLocal()`, прямые обращения
- `main.js`: `readLocalState()`, `writeLocalState()`, `_localKeyForUser()`
- `sync-cloud.js`: `readLocalState()`, `writeLocalState()` (другая реализация)

**API вызовы:**
- `app-main.js`: прямые `fetch()` вызовы
- `main.js`: `getJSON()`, `postJSON()` обёртки
- Нет единого API клиента

#### 1.3 Глобальные переменные
```javascript
let tx = [];           // Транзакции
let bills = [];        // Счета
let kasa = [];         // Касса
let accMeta = {};      // Метаданные счетов
let _trendSeries = null;
let syncTimer = null;
let syncing = false;
```
- Нет инкапсуляции
- Риск конфликтов имён
- Сложно отслеживать изменения
- Нет единого источника истины (state)

#### 1.4 Нарушение принципов SOLID

**SRP (Single Responsibility):**
- `render()` функция делает: KPI, таблицы, графики, модалки, валидацию
- `app-main.js` отвечает за всё приложение

**DRY (Don't Repeat Yourself):**
- Дублирование i18n
- Дублирование работы с localStorage
- Дублирование API вызовов
- Дублирование парсинга CSV

**KISS (Keep It Simple):**
- Сложная логика синхронизации (3 разных места)
- Смешение форматов данных (старый/новый blob)
- Множество условных проверок для совместимости

### 2. Качество кода

#### 2.1 Code Smells

**Long Method:**
- `render()` - 200+ строк
- `DOMContentLoaded` handler - 450+ строк
- `buildTrendSeries()` - сложная логика

**Large Class (файл):**
- `app-main.js` - 2961 строка

**Feature Envy:**
- Множество обращений к `localStorage` напрямую
- Прямые DOM манипуляции вместо компонентов

**Primitive Obsession:**
- Работа с данными через строковые ключи: `getVal(r, ["Status faktury", "Status фактуры", "Status"])`
- Нет типизированных моделей

**Magic Numbers:**
```javascript
end.setMonth(end.getMonth() + 2);  // Что такое 2?
const iter = 100000;                 // Почему именно это число?
const keylen = 64;                   // Откуда взялось?
```

**Hardcoded Values:**
- Firebase конфигурация в 3 местах (app.html, index.html, sync-cloud.js)
- API endpoints: `/api`, `/app-state`, `/create-checkout-session`
- Ключи localStorage: `'otd_sub_active'`, `'otd_user'`, и т.д.

#### 2.2 Обработка ошибок

**Проблемы:**
- Много `try-catch` с пустыми блоками: `catch(e){}`
- `console.warn()` вместо proper error handling
- Нет централизованной обработки ошибок
- Нет retry логики для API вызовов
- Нет обработки race conditions

**Примеры:**
```javascript
try { kasa=JSON.parse(localStorage.getItem('kasa')||'[]'); } catch(e){kasa=[]}
// Что если localStorage недоступен? Что если данные повреждены?
```

#### 2.3 Асинхронность

**Проблемы:**
- Нет обработки race conditions
- Множественные параллельные `fetch()` без координации
- Нет debounce/throttle для синхронизации
- `scheduleAutosync()` может создавать множественные таймеры
- Нет отмены запросов (AbortController)

**Пример:**
```javascript
async function ocrBankFiles(files){
  for(const f of files){
    // Нет проверки, что предыдущий запрос завершился
    const text = await recognizeImage(f);
  }
}
```

### 3. Работа со стейтом

#### 3.1 Проблемы

**Множественные источники истины:**
1. `localStorage` (разные ключи для разных данных)
2. `server.js` → `users[email].appState`
3. Firebase Realtime Database
4. Глобальные переменные `tx`, `bills`, `kasa`

**Синхронизация:**
- 3 разных механизма синхронизации:
  - `/app-state` endpoint
  - `/app-state/merge` endpoint  
  - Firebase через `sync-cloud.js`
- Нет конфликт-резолюции
- Нет версионирования данных
- Может потеряться состояние при конфликтах

**Мутации:**
- Прямые мутации глобальных массивов: `tx.push(...)`, `bills = bills.filter(...)`
- Нет иммутабельности
- Сложно отследить, где и что изменилось

**Реактивность:**
- Нет системы реактивности
- `render()` вызывается вручную после каждого изменения
- Могут быть пропущены обновления UI

### 4. Работа с асинхронностью

#### 4.1 Fetch/API

**Проблемы:**
- Нет единого API клиента
- Дублирование логики запросов
- Нет обработки таймаутов
- Нет retry механизма
- Нет обработки offline режима

**Примеры:**
```javascript
// app-main.js
const r = await fetch('/app-state', { credentials:'include' });

// main.js  
async function getJSON(path){
  const r = await fetch((apiBase||'') + path, { credentials:'include' });
  // Разная обработка ошибок
}
```

#### 4.2 Race Conditions

**Проблемы:**
- `scheduleAutosync()` может запустить несколько синхронизаций
- Нет блокировки во время синхронизации
- Параллельные обновления могут конфликтовать

```javascript
let syncing = false;  // Примитивная защита, но не везде используется
```

### 5. Неиспользуемые файлы

- `public/i18n_pack/i18n.js` - не используется (есть своя реализация)
- `config/` - пустая папка
- `uploads/` и `data/uploads/` - дубликаты?

### 6. Другие проблемы

- **Нет TypeScript** - нет типизации, сложнее отлавливать ошибки
- **Нет тестов** - нет unit/integration тестов
- **Нет документации** - нет JSDoc комментариев
- **925 console.log/warn** - много отладочного кода
- **Нет линтера** - нет ESLint/Prettier конфигурации
- **Смешение языков** - комментарии на русском, польском, английском

---

## 📝 План рефакторинга

### Фаза 1: Подготовка и очистка (Приоритет: ВЫСОКИЙ)

#### 1.1 Удаление дубликатов и мусора
- [ ] Удалить неиспользуемый `public/i18n_pack/i18n.js`
- [ ] Проверить и удалить дубликаты папок (`uploads/` vs `data/uploads/`)
- [ ] Удалить пустую папку `config/` или использовать её
- [ ] Удалить закомментированный код
- **Результат:** Чистая структура проекта

#### 1.2 Единая i18n система
- [ ] Выбрать один формат (JSON файлы)
- [ ] Создать единый модуль `src/i18n/index.js`
- [ ] Мигрировать все переводы в JSON
- [ ] Удалить дублирующие объекты `M` и `T`
- [ ] Обновить все места использования
- **Результат:** Одна система i18n, все переводы в JSON

#### 1.3 Единый API клиент
- [ ] Создать `src/api/client.js` с методами `get()`, `post()`, `put()`, `delete()`
- [ ] Добавить обработку ошибок, таймауты, retry
- [ ] Заменить все прямые `fetch()` на использование клиента
- **Результат:** Единая точка для всех API вызовов

### Фаза 2: Разделение ответственности (Приоритет: ВЫСОКИЙ)

#### 2.1 Разбить app-main.js на модули

**Структура:**
```
src/
├── core/
│   ├── state.js           # Управление состоянием
│   ├── storage.js         # Абстракция над localStorage
│   └── sync.js            # Синхронизация (Firebase + Server)
├── services/
│   ├── api.js             # API клиент
│   ├── auth.js            # Аутентификация
│   ├── transactions.js    # Работа с транзакциями
│   ├── bills.js           # Работа со счетами
│   ├── cash.js            # Работа с кассой
│   └── accounts.js        # Работа со счетами
├── utils/
│   ├── csv.js             # Парсинг CSV
│   ├── ocr.js             # OCR обработка
│   ├── currency.js         # Работа с валютами
│   ├── dates.js            # Работа с датами
│   └── validation.js      # Валидация
├── ui/
│   ├── components/        # UI компоненты
│   │   ├── Table.js
│   │   ├── Modal.js
│   │   ├── Chart.js
│   │   └── ...
│   ├── renderers/         # Рендереры для разных секций
│   │   ├── dashboard.js
│   │   ├── transactions.js
│   │   ├── bills.js
│   │   └── ...
│   └── i18n.js            # i18n для UI
└── app.js                 # Главный файл приложения
```

**Задачи:**
- [ ] Создать структуру папок
- [ ] Выделить работу с данными в `services/`
- [ ] Выделить UI логику в `ui/`
- [ ] Выделить утилиты в `utils/`
- [ ] Создать единый state manager в `core/state.js`
- **Результат:** Модульная структура, каждый модуль < 300 строк

#### 2.2 State Management
- [ ] Создать `core/state.js` с единым состоянием
- [ ] Использовать паттерн Observer для реактивности
- [ ] Иммутабельные обновления состояния
- [ ] Заменить глобальные переменные на state
- **Результат:** Единый источник истины, реактивные обновления UI

#### 2.3 Storage абстракция
- [ ] Создать `core/storage.js` с методами:
  - `get(key)`, `set(key, value)`, `remove(key)`
  - `getUserState()`, `saveUserState(state)`
- [ ] Инкапсулировать работу с localStorage
- [ ] Добавить обработку ошибок (quota exceeded, etc.)
- [ ] Заменить все прямые обращения к localStorage
- **Результат:** Единый интерфейс для работы с хранилищем

### Фаза 3: Улучшение качества кода (Приоритет: СРЕДНИЙ)

#### 3.1 Обработка ошибок
- [ ] Создать `src/utils/errors.js` с классами ошибок
- [ ] Централизованный error handler
- [ ] Добавить retry логику для API
- [ ] Улучшить обработку в критических местах
- [ ] Заменить `console.warn` на proper error handling
- **Результат:** Надёжная обработка ошибок, пользователь видит понятные сообщения

#### 3.2 Асинхронность
- [ ] Добавить debounce для синхронизации
- [ ] Использовать AbortController для отмены запросов
- [ ] Добавить обработку race conditions
- [ ] Улучшить `scheduleAutosync()` (предотвратить множественные таймеры)
- **Результат:** Надёжная работа с асинхронностью, нет race conditions

#### 3.3 Рефакторинг больших функций
- [ ] Разбить `render()` на отдельные функции по секциям
- [ ] Разбить `DOMContentLoaded` handler на инициализацию модулей
- [ ] Вынести сложную логику в отдельные функции
- **Результат:** Функции < 50 строк, легко тестировать

#### 3.4 Убрать хардкод
- [ ] Вынести конфигурацию в `src/config/index.js`
- [ ] Использовать environment variables
- [ ] Создать константы для магических чисел
- [ ] Вынести API endpoints в конфиг
- **Результат:** Конфигурация в одном месте, легко менять

### Фаза 4: Синхронизация и данные (Приоритет: СРЕДНИЙ)

#### 4.1 Единая система синхронизации
- [ ] Создать `core/sync.js` с единой логикой
- [ ] Объединить Firebase и Server синхронизацию
- [ ] Добавить конфликт-резолюцию
- [ ] Добавить версионирование данных
- [ ] Оптимизировать частоту синхронизации
- **Результат:** Надёжная синхронизация, нет потери данных

#### 4.2 Типизация данных
- [ ] Создать модели данных (Transaction, Bill, CashEntry, Account)
- [ ] Добавить валидацию при создании/обновлении
- [ ] Использовать единые ключи вместо множественных вариантов
- [ ] Создать адаптеры для старых форматов
- **Результат:** Типизированные данные, меньше ошибок

### Фаза 5: UI и компоненты (Приоритет: НИЗКИЙ)

#### 5.1 Компонентная архитектура
- [ ] Создать базовые UI компоненты (Button, Input, Table, Modal)
- [ ] Выделить переиспользуемые компоненты
- [ ] Разделить логику и представление
- **Результат:** Переиспользуемые компоненты, легче поддерживать

#### 5.2 Улучшение рендеринга
- [ ] Использовать виртуальный DOM или шаблонизатор
- [ ] Оптимизировать перерисовки (только изменённые части)
- [ ] Добавить loading states
- **Результат:** Плавный UI, нет лишних перерисовок

### Фаза 6: Инфраструктура (Приоритет: НИЗКИЙ)

#### 6.1 TypeScript (опционально)
- [ ] Добавить TypeScript
- [ ] Постепенная миграция (allowJs)
- [ ] Типизировать критические модули
- **Результат:** Меньше ошибок, лучший DX

#### 6.2 Тестирование
- [ ] Настроить Jest/Vitest
- [ ] Написать unit тесты для утилит
- [ ] Написать integration тесты для критических путей
- **Результат:** Уверенность в изменениях

#### 6.3 Линтинг и форматирование
- [ ] Настроить ESLint
- [ ] Настроить Prettier
- [ ] Добавить pre-commit hooks
- **Результат:** Единый стиль кода

---

## 🏗️ Рекомендованная архитектура

### Целевая структура проекта

```
onetapday-mvp/
├── server/
│   ├── index.js              # Точка входа
│   ├── routes/               # API routes
│   │   ├── auth.js
│   │   ├── users.js
│   │   ├── app-state.js
│   │   └── stripe.js
│   ├── middleware/           # Express middleware
│   │   ├── auth.js
│   │   └── errors.js
│   ├── services/             # Бизнес-логика
│   │   ├── user-service.js
│   │   └── state-service.js
│   ├── utils/                # Утилиты
│   │   ├── crypto.js
│   │   └── validation.js
│   └── config/               # Конфигурация
│       └── index.js
│
├── src/                      # Frontend
│   ├── index.js              # Точка входа
│   ├── app.js                # Главный компонент
│   │
│   ├── core/                 # Ядро приложения
│   │   ├── state.js          # State management
│   │   ├── storage.js        # Storage абстракция
│   │   └── sync.js           # Синхронизация
│   │
│   ├── services/             # Сервисы
│   │   ├── api.js            # API клиент
│   │   ├── auth.js           # Аутентификация
│   │   ├── transactions.js   # Транзакции
│   │   ├── bills.js          # Счета
│   │   ├── cash.js           # Касса
│   │   └── accounts.js       # Счета
│   │
│   ├── models/               # Модели данных
│   │   ├── Transaction.js
│   │   ├── Bill.js
│   │   ├── CashEntry.js
│   │   └── Account.js
│   │
│   ├── ui/                   # UI слой
│   │   ├── components/       # Компоненты
│   │   │   ├── Button.js
│   │   │   ├── Input.js
│   │   │   ├── Table.js
│   │   │   ├── Modal.js
│   │   │   └── Chart.js
│   │   ├── pages/            # Страницы/секции
│   │   │   ├── Dashboard.js
│   │   │   ├── Transactions.js
│   │   │   ├── Bills.js
│   │   │   ├── Cash.js
│   │   │   └── Settings.js
│   │   └── layout/           # Layout компоненты
│   │       ├── Header.js
│   │       └── Navigation.js
│   │
│   ├── utils/                # Утилиты
│   │   ├── csv.js
│   │   ├── ocr.js
│   │   ├── currency.js
│   │   ├── dates.js
│   │   └── validation.js
│   │
│   ├── i18n/                 # Интернационализация
│   │   ├── index.js
│   │   └── locales/
│   │       ├── pl.json
│   │       ├── en.json
│   │       ├── ru.json
│   │       └── uk.json
│   │
│   └── config/               # Конфигурация
│       └── index.js
│
├── public/                   # Статические файлы
│   ├── index.html
│   ├── app.html
│   ├── manifest.json
│   └── assets/
│
├── tests/                    # Тесты
│   ├── unit/
│   └── integration/
│
├── package.json
├── tsconfig.json            # Если TypeScript
├── .eslintrc.js
├── .prettierrc
└── README.md
```

### Принципы архитектуры

#### 1. **Feature-Sliced Design** (упрощённый)
- Разделение по слоям: `core` → `services` → `ui`
- Каждый слой зависит только от слоёв ниже
- Изоляция бизнес-логики от UI

#### 2. **SOLID принципы**
- **S** - каждый модуль отвечает за одну вещь
- **O** - открыт для расширения, закрыт для модификации
- **L** - подстановка Лисков (интерфейсы)
- **I** - разделение интерфейсов
- **D** - зависимость от абстракций

#### 3. **DRY (Don't Repeat Yourself)**
- Единая система i18n
- Единый API клиент
- Единая система хранения
- Переиспользуемые компоненты

#### 4. **KISS (Keep It Simple)**
- Простые функции (< 50 строк)
- Понятные имена
- Минимум абстракций
- Явные зависимости

### State Management

**Рекомендация:** Простой Observer паттерн или Zustand (если нужна библиотека)

```javascript
// core/state.js
class AppState {
  constructor() {
    this.state = {
      transactions: [],
      bills: [],
      cash: [],
      accounts: {}
    };
    this.listeners = [];
  }
  
  subscribe(listener) {
    this.listeners.push(listener);
    return () => {
      this.listeners = this.listeners.filter(l => l !== listener);
    };
  }
  
  setState(updates) {
    this.state = { ...this.state, ...updates };
    this.listeners.forEach(l => l(this.state));
  }
  
  getState() {
    return this.state;
  }
}
```

### Синхронизация

**Стратегия:**
1. Локальное состояние - источник истины для UI
2. Фоновая синхронизация с сервером
3. Конфликт-резолюция: Last-Write-Wins или пользователь выбирает
4. Оптимистичные обновления UI

---

## 📊 Метрики качества

### До рефакторинга:
- Размер самого большого файла: **2961 строка**
- Количество глобальных переменных: **7+**
- Дублирование кода: **~30%**
- Покрытие тестами: **0%**
- Цикломатическая сложность: **Высокая**

### Целевые метрики:
- Размер самого большого файла: **< 300 строк**
- Количество глобальных переменных: **0** (все в state)
- Дублирование кода: **< 5%**
- Покрытие тестами: **> 60%** (критические пути)
- Цикломатическая сложность: **< 10** на функцию

---

## ⚠️ Важные замечания

1. **Постепенная миграция** - не переписывать всё сразу, делать пошагово
2. **Обратная совместимость** - сохранить работу со старыми данными
3. **Тестирование** - тестировать после каждого этапа
4. **Резервные копии** - делать бэкапы перед большими изменениями
5. **Документация** - документировать изменения в архитектуре

---

## 🎯 Приоритеты выполнения

1. **КРИТИЧНО** (Фаза 1-2): Очистка, единые системы, разделение ответственности
2. **ВАЖНО** (Фаза 3-4): Качество кода, синхронизация
3. **ЖЕЛАТЕЛЬНО** (Фаза 5-6): UI компоненты, инфраструктура

---

*Отчёт составлен: 2024*
*Версия проекта: MVP*

