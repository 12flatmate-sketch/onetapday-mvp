# Отчет об исправлении системы i18n

## Дата: 2024

## Проблемы, которые были найдены

### 1. Два конфликтующих движка i18n
- **Старый движок**: `data-i` атрибуты + словари `M.*` в JavaScript (app-main.js, ~550 строк)
- **Новый движок**: `data-i18n` атрибуты + JSON-файлы в `/public/i18n_pack/i18n/`
- **Конфликт**: Оба движка работали одновременно, перезаписывали переводы и ломали инициализацию

### 2. Неполные JSON-файлы
- JSON-файлы в `/public/i18n_pack/i18n/` содержали дублирующиеся объекты
- Многие ключи отсутствовали
- Структура не соответствовала канону из `/languages`

### 3. Неправильные пути
- В `i18n.js` был указан путь `/public/i18n/` вместо `/i18n_pack/i18n/`
- В `app.html` был указан путь `/public/i18n_pack/i18n.js` вместо `/i18n_pack/i18n.js`

### 4. Использование старого движка в коде
- В `app-main.js` использовались словари `M.*` (строки 18-563)
- Функции `applyLang()` и `applyPlaceholders()` работали со старым движком
- Использование `M[lang]` в коде (строки 1171, 2079)

## Выполненные изменения

### 1. Синхронизация JSON-файлов с каноном ✅
- **Файлы обновлены**: 
  - `/public/i18n_pack/i18n/en.json`
  - `/public/i18n_pack/i18n/pl.json`
  - `/public/i18n_pack/i18n/ru.json`
  - `/public/i18n_pack/i18n/uk.json`
- **Результат**: Все файлы теперь содержат полный набор ключей из канона `/languages`
- **Структура**: Единая структура, правильная вложенность, единый формат

### 2. Исправление путей ✅
- **i18n.js**: Исправлен путь с `/public/i18n/` на `/i18n_pack/i18n/`
- **app.html**: Исправлен путь с `/public/i18n_pack/i18n.js` на `/i18n_pack/i18n.js`
- **Результат**: Пути теперь корректны для сервера Express (public как корень статики)

### 3. Обновление i18n.js ✅
- Добавлена поддержка `data-i18n-ph` для placeholder'ов
- Добавлена функция `i18n.t(key)` для программного доступа к переводам
- Исправлен ключ localStorage с `lang` на `otd_lang` для совместимости
- Добавлена обработка ошибок загрузки

### 4. Замена data-i на data-i18n в app.html ✅
- **Всего заменено**: ~174 использования `data-i=` на `data-i18n=`
- **Placeholder'ы**: Все `data-i-ph=` заменены на `data-i18n-ph=`
- **Результат**: HTML теперь полностью использует новый движок

### 5. Удаление старого движка из app-main.js ✅
- **Удалено**: Весь объект `M` со словарями (строки 18-563, ~550 строк кода)
- **Удалено**: Функция `applyPlaceholders()` (старая версия)
- **Переписано**: Функция `applyLang()` для использования нового i18n
- **Заменено**: Все использования `M[lang]` на `window.i18n.t()`
  - Строка 1171: синхронизация
  - Строка 2079: AI quick questions

### 6. Проверка других файлов ✅
- **index.html**: Использует свою систему i18n (объект `T` в main.js) - оставлена без изменений, так как это отдельная landing page
- **main.js**: Содержит свою систему i18n для landing page - не конфликтует с основной системой

## Список измененных файлов

1. `/public/i18n_pack/i18n/en.json` - полностью переписан
2. `/public/i18n_pack/i18n/pl.json` - полностью переписан
3. `/public/i18n_pack/i18n/ru.json` - полностью переписан
4. `/public/i18n_pack/i18n/uk.json` - полностью переписан
5. `/public/i18n_pack/i18n.js` - обновлен (пути, функциональность)
6. `/public/app.html` - заменены все `data-i` на `data-i18n` (~174 замены)
7. `/public/js/app-main.js` - удалены словари M.*, переписаны функции (~550 строк удалено)

## Список удаленных ключей

Все ключи из старого движка были удалены, так как они теперь находятся в JSON-файлах. Старые ключи включали:
- Плоские ключи (например, `tab_dashboard`, `gate_locked_title`)
- Вложенные ключи (например, `ai.profile_who_solo`, `home.tagline`)

Все эти ключи теперь доступны через новую структуру JSON.

## Список добавленных ключей

Все ключи из канона `/languages` теперь присутствуют во всех языках:
- `app.*` - информация о приложении
- `nav.*` - навигация
- `home.*` - главная страница
- `documents.*` - документы
- `money.*` - деньги и платежи
- `ai.*` - AI-бухгалтер
- `reports.*` - отчеты
- `cash.*` - касса
- `accounts.*` - счета
- `kpi.*` - KPI метрики
- `plan.*` - план платежей
- `spending.*` - расходы
- `spcat.*` - категории расходов
- `tables.*` - таблицы
- `settings.*` - настройки
- `buttons.*` - кнопки
- `placeholders.*` - placeholder'ы
- `modal.*` - модальные окна
- `errors.*` - ошибки

## Финальный чеклист для проверки i18n

### ✅ Выполнено
- [x] JSON-файлы синхронизированы с каноном
- [x] Все пути исправлены
- [x] Старый движок полностью удален
- [x] Все `data-i` заменены на `data-i18n`
- [x] Код переписан на новый i18n
- [x] Функции обновлены

### 🔍 Требуется проверка
- [ ] Проверить работу переключения языков в UI
- [ ] Проверить, что все переводы отображаются корректно
- [ ] Проверить работу placeholder'ов
- [ ] Проверить работу программного доступа через `i18n.t()`
- [ ] Проверить, что нет ошибок в консоли браузера
- [ ] Проверить загрузку JSON-файлов (Network tab)

## Рекомендации

1. **Тестирование**: Необходимо протестировать все страницы приложения с разными языками
2. **Добавление новых ключей**: При добавлении новых ключей обновлять все 4 языка одновременно
3. **Валидация**: Рассмотреть добавление валидации JSON-файлов при сборке
4. **Документация**: Обновить документацию для разработчиков о том, как добавлять новые переводы

## Примечания

- Landing page (`index.html` + `main.js`) использует свою систему i18n и не была изменена, так как не конфликтует с основной системой
- Старый движок полностью удален из основного приложения
- Новый движок использует JSON-файлы и автоматически применяет переводы при загрузке страницы

